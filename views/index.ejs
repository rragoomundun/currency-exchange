<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Currency Exchange</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="/styles/main.css" />
  </head>
  <body>
    <main>
      <form method="GET" action="/">
        <div class="mb-3">
          <h1 class="text-center">Currency Exchange</h1>
        </div>

        <div class="mb-3">
          <input
            type="number"
            class="form-control"
            placeholder="Amount"
            name="amount"
            value="<%= amount %>"
            step=".01"
          />
        </div>
        <div class="mb-3">
          <input
            type="text"
            class="form-control"
            placeholder="Initial currency"
            name="initialCurrency"
            value="<%= initialCurrency %>"
          />
        </div>
        <div class="mb-3">
          <input
            type="text"
            class="form-control"
            placeholder="End currency"
            name="endCurrency"
            value="<%= endCurrency %>"
          />
        </div>
        <div class="mb-3 text-center">
          <button class="btn btn-primary">Convert</button>
        </div>
      </form>

      <% if (locals.rate && locals.endAmount) { %>
      <hr />
      <p><%= amount %> <%= initialCurrency %> equals <%= endAmount %> <%= endCurrency %> (rate: <%= rate %>)</p>
      <% } %> <% if (locals.error) { %>
      <hr />
      <p>Error: cannot make conversion</p>
      <% } %>
    </main>

    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
