A simple currency exchange application. Enter an initial currency, the amount, and the final currency to get a conversion.

## Install the dependencies
    npm i

## Run the app
    nodemon server.js
